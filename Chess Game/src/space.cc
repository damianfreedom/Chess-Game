#include <space.h>

namespace chess {
    bool Space::Move(int new_x_position, int new_y_position, const Board &board) {
        return true;
    }

    bool Space::CheckPossibleMove(int new_x_position, int new_y_position, const Board &board) {
        return true;
    }
}

